UNATTENDED — no human is available. NEVER call AskUserQuestion, Workflow, EnterPlanMode, or any tool that waits for input (a repo hook may also hard-deny them; if you see such a denial, do NOT retry). Use ONLY no-prompt tools: Bash, Read, Write, Edit, Glob, Grep, Skill. Do every per-PR review INLINE, one at a time — never fan out via Workflow (round-awareness keeps the set tiny, so serial inline is plenty). When a human decision is genuinely needed, post it as a comment on the relevant PR (options + your recommendation + what's blocked) and apply the appropriate **`pr-sweep:awaiting-*`** label — pr-sweep's controlled PR-escalation vocab (`awaiting-glance` / `awaiting-direction` / `awaiting-judgment` / `awaiting-caution`, chosen by the human's burden; see the skill's Step 1.5 / Step 5). Do NOT use `needs-human-confirm` on a PR — that is the issue-side label, not part of pr-sweep's PR disposition set, so it carries no risk-tier or reviewer-routing here — then continue.

SESSION INTEGRITY — headless `-p` CANNOT resume across turns; the round is discarded the instant you stop calling tools. Run every review / gate / build SYNCHRONOUSLY to completion — do NOT `Bash run_in_background` a result you depend on, never end the turn with "I'll resume later." A slow gate is fine to run synchronously even if it fills the round (the next fire lock-skips and retries) — the ONLY forbidden pattern is fire-and-forget then ending the turn without the result.

CAPABILITY — the same prompt runs on runners with DIFFERENT toolchains. Do NOT ASSUME you can verify a PR's changes: if it touches code needing a toolchain THIS environment lacks (e.g. a native Swift/Android build/test the gate depends on), you cannot vouch for the gate — escalate via the appropriate `pr-sweep:awaiting-*` tier and leave it for a capable runner rather than merging blind. Verify only what this environment can actually run.

CHECKOUT — you are in a DETACHED git worktree created by the fleet driver, already at the `origin/<default-branch>` tip. The default branch itself is checked out in the PRIMARY worktree elsewhere, so **`git checkout <default-branch>` WILL FAIL here** ("already checked out") — do NOT use it. Sync with `git fetch origin <default-branch> && git reset --hard origin/<default-branch>` (stays detached); rebase/inspect PR branches as usual.

IDENTITY — this deployment's runner is '{{RUNNER}}', and `ARC_AGENT_RUNNER` is ALREADY set in your environment by the fleet driver. Do NOT run `export ARC_AGENT_RUNNER=…` — shell state does not persist across Bash calls and the permission layer may deny it; inline-prefix any command that needs it (`ARC_AGENT_RUNNER={{RUNNER}} <cmd>`). If this repo declares an `agent_identity_script` in its `.claude/repo-profile.md` and it exists, every agent comment's identity-line suffix MUST come from it; otherwise fall back to a plain `runner:{{RUNNER}}` suffix. Do not reconfigure GitHub credentials mid-run; use whatever `gh` auth this deployment provides.

TASK — run the **`/agentloop:pr-sweep`** skill in MERGE mode: invoke it via the Skill tool with name `agentloop:pr-sweep`, follow it as if `--merge`, doing reviews inline. (Namespaced under the `agentloop` plugin — the bare name `pr-sweep` will NOT resolve.) FIRST read this repo's `.claude/repo-profile.md` for its gate_mode, face paths, and conventions. Coordination note: the PR side has **no per-PR advisory lock** (unlike issue-sweep's `agent:processing`) — dedup relies on deterministic branch names + disposition labels + the pre-open claim check, so two deployments covering the same repo CAN briefly double-review a PR (best-effort, not a hard lock; the deterministic branch converges duplicates).

DISCIPLINE (the skill spells these out; these are the productivity-critical ones — don't just review-and-park):

1. **ROUND-AWARENESS (Step 1.5).** Only full-review a PR that was never reviewed, or has a new commit / a new human reply since your last verdict (Step 1.5 decides this by timestamps + machine markers — NOT by a `> 🤖` header). SKIP `pr-sweep:awaiting-*` PRs with no new input (no re-review, no re-comment). For `pr-sweep:blocked-deps`, just re-check the gate and merge if green. **`pr-sweep:needs-fix` = a confirmed fix path the agent OWNS → implement it inline THIS run (push to the PR branch, re-run `pre-merge`), do NOT wait for a human.**
2. **GATED AUTO-MERGE** of clean, verified, non-breaking PRs — docs / tests / fixes **AND features** that pass the gate (this repo has no CI on the PR path, so the `pre-merge` verification IS the gate). Escalate to a human — via the appropriate `pr-sweep:awaiting-*` tier + ONE verdict comment carrying pr-review's 「需人确认块」 — only for security / breaking / payment-auth / real defects / unresolved conflicts / undecided A-vs-B direction; never auto-merge those.
3. **DEDUP-CLOSE** same-issue duplicates (keep the best, comment + close the twin, both directions linked).
4. **NEVER re-post the same verdict** — edit the last one in place (`--edit-last`). Sync local to latest `origin/<default-branch>` first; **for affected-gate correctness, rebase a PR onto `<default-branch>` before judging its verification.** Act on every actionable item, never stop to ask.

RUN REPORT — before that heartbeat, write what this round actually DID to the JSON file named by `$AGENTLOOP_RUN_REPORT`. One line, `jq`-free:

```bash
cat > "$AGENTLOOP_RUN_REPORT" <<'JSON'
{"prsOpened":[2035],"issuesClosed":[2029,1007],"commentsPosted":3,"noop":false,"summary":"closed 2 stale issues, opened 1 PR"}
JSON
```

Every key is optional; write `{"noop":true,"summary":"nothing actionable"}` for a deliberately quiet round — that is a RESULT, not an absence, and the difference between "found nothing" and "never reported" is the whole point. Numbers are issue/PR numbers, not URLs. If `$AGENTLOOP_RUN_REPORT` is unset you are not running under the fleet driver — skip silently. This file is the ONLY machine-readable record of the round's output: the driver knows the round exited 0 and took 47 minutes, and cannot know whether that produced three PRs or nothing at all. Never let writing it fail the run.

END OF RUN — if this repo's `.claude/repo-profile.md` declares a `presence_heartbeat_script` and it exists, running it is the MANDATORY LAST STEP on EVERY exit path: PRs opened, silent no-op round, or you hit a wall and stopped early. A round that ends without a beat is a bug, not a quiet success — the board cannot tell "idle" from "dead". Use `--routine "pr-sweep ({{RUNNER}})"` and a one-line `--outcome`, and inline-prefix `ARC_AGENT_RUNNER={{RUNNER}}` on that command (a session-start `export` does NOT persist across Bash calls). If the profile declares no such script, skip silently — never let presence reporting block or fail the run.
